# 2.6 验证记录

日期：2026-09-18。版本 `2.6` / versionCode `15`。

## 本地验证

使用 Android Studio JBR，在英文目录源码副本执行 `testDebugUnitTest lintDebug assembleDebug assembleRelease`。中文工作目录的测试因宿主路径问题统一出现 `ClassNotFoundException`，不计作通过；源码副本对 84 个源码、资源、测试及构建配置文件逐文件核对 SHA-256。

- 122 项单元测试：121 通过、0 失败、1 跳过。跳过已有 Windows FileProvider 路径语义测试，Linux CI 负责执行。
- Lint：0 errors、34 warnings，保留原有警告；新增文案未增加警告。
- Debug、未签名 Release、隔离验收 Debug 与 AndroidTest APK 构建通过。
- 新增覆盖：收藏跨实例持久化、删除入口保护、未知 JSON 字段保留、不可读元数据不覆盖、旧 WAV 无迁移；ZIP 跨目录同名文件与重复选择、逐字节原音频、损坏来源、复制失败、取消及目标清理失败；批量删除并发互斥、部分失败、元数据重试、选择刷新、收藏保存失败、文件选择器请求重建；午夜和正午的夜间分组边界。

## 模拟器交互

Android 11 / API 30，`lcg_113_api30_probe`，320 × 640；只安装 `.acceptance` 隔离测试包，不安装正式包、不做真机验证。

- `HistoryAcceptanceTest` 3 项通过，包括已有 500 段同晚懒加载/播放/菜单身份回归、多晚列表与重建、新增多选重建/取消确认/收藏保护/取消收藏。
- 新增专项补测系统 ZIP 文件选择器打开及取消，原件保留；再次执行通过。空间概览与删除确认截图已检查。
- 系统 Gboard 崩溃弹窗曾遮挡文件选择器，导致新增选择器断言失败。临时禁用模拟器 Gboard 后复验通过，随后重新启用；没有修改应用规避宿主问题。

## 数据与发布边界

空间数值仅统计当前可访问录音目录中的 WAV；不含元数据、未完成片段或分享缓存，不代表手机全部存储。

收藏不会影响回听、分享或 ZIP 导出。删除在持有录音元数据锁时重新检查收藏；收藏更新沿用同目录临时文件、同步、原子替换。批量删除可能部分成功，并显示结果及重试入口。ZIP 导出不修改源文件；损坏来源使整次导出失败，不静默漏项。

本轮用户明确授权直接发布，并免除真机验证与发布附件重新下载。发布遵循 PR → CI → 合并 → 附注标签 `v2.6` → Release 工作流；工作流验证签名连续性、正式包名、版本和不可调试状态，再生成 APK 与 `SHA256SUMS.txt`。以 GitHub 工作流和 Release API 的附件名称、数量、大小、摘要验证发布，不重新下载。模拟器证据不代表真机或所有厂商整夜录音验收。
