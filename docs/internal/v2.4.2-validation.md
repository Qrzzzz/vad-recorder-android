# 2.4.2 验证记录

日期：2026-09-17。范围：#14、#15、#20、#22。版本 `2.4.2` / versionCode `12`。

## 本地自动验证

使用 Android Studio JBR，在英文路径的当前源码副本运行：

```powershell
.\gradlew.bat testDebugUnitTest lintDebug assembleDebug assembleRelease assembleDebugAndroidTest --console=plain
```

86 项测试，85 通过、0 失败、1 跳过。跳过的是已有 FileProvider 跨应用 URI 测试：Windows 不具备 Android/Unix 路径语义；Linux CI 执行该测试。Lint 0 errors、34 warnings。Debug、未签名 Release 及仪器测试 APK 构建通过。

原中文路径下发生所有测试类 ClassNotFoundException，未将该结果计为通过。源码、资源、测试和构建配置共 71 个文件与英文路径副本逐文件 SHA-256 一致。aapt 确认正式包名 `com.qrz.voicetriggerrecorder`、2.4.2 / 12、minSdk 26、targetSdk 35。

## 回归覆盖

- 完整旧 WAV、截断数据、缺失全部数据、零样本及不存在文件的分类；列表和传输判断一致，列表不新建或改写历史 JSON。
- 内部回退目录到外部可用的切换、两个目录同名文件、来源消失、可信根外路径及路径穿越。分享快照和导出的字节来自选定来源，删除不影响同名另一文件。
- 临时 JSON 被部分写入后抛异常，以及临时文件消失导致原子提交失败；正式 JSON 保持旧内容，音频保留，后续重试可成功。首次写入失败返回 false。
- 用线程屏障交错写入和读取，读取等待提交且保留确定时间和 speechDurationMs。
- 注入音频删除失败、元数据删除失败和目标已不存在；音频失败不动 JSON，部分成功可通过原始身份重试清理。
- 原有录音状态机、生命周期、回听、传输和时间合并回归通过。

## 验收边界与发布核验

`adb devices` 无连接设备，未执行仪器测试、真机 UI、真实挂载变化、磁盘耗尽或掉电测试。自动测试使用隔离目录及故障注入，不访问用户录音。未迁移或批量重写历史 WAV/JSON。

发布工作流执行测试、Lint、Release 构建，并在发布前校验签名与既有证书连续性、正式包名、版本和非 debuggable 属性。发布后通过 GitHub API 核对标签提交、工作流、附件及 digest；按用户要求不重新下载附件。
